node{
	//properties([pipelineTriggers([pollSCM('* * * * *')])])
	checkout scm
    stage('build') {
        echo "build"

    }
    stage('run') {
        echo "run again and again and again"
	echo "hello 2"
    }
}
